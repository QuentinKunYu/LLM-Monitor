# Optional provider credentials for live experiment runs.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional password required to start or retry a run. Leave blank to disable.
APP_PASSWORD=

# Optional persistent storage location. Defaults to ./data locally.
DATA_DIR=
